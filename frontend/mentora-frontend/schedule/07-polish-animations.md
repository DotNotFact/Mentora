# 07 — Polish & Animations

Статус: [ ] не начата

## Цель

Пройтись по всему приложению и привести переходы/анимации/доступность к
единому стандарту из `.agents/skills/mentora-design/SKILL.md`, не ломая
функциональность предыдущих задач.

## Ветка

`feature/07-polish-animations`

## Шаги

1. Аудит всех интерактивных элементов на `transition-all duration-150` по
   умолчанию (кнопки, ссылки, инпуты).
2. Проверить, что `hover:scale-[1.02]` используется только у `CourseCard`
   и нигде больше.
3. Модалки/диалоги (shadcn/ui Dialog) — `animate-in fade-in zoom-in-95`.
4. Sidebar (course-editor, layout) — `transition-[width] duration-200`.
5. Списки >10 элементов (каталог курсов, аналитика) — убедиться, что НЕ
   анимируются width/height (только opacity/transform при необходимости).
6. Добавить `prefers-reduced-motion` media-query: отключать/сокращать
   анимации для пользователей с этой настройкой ОС.
7. Клавиатурная навигация: пройти Tab по ключевым потокам (login, каталог,
   checkout, редактор курса) — фокус-кольца видимы, порядок логичен.
8. Aria-labels для иконок-кнопок без текста (header, sidebar, video player
   controls).
9. Проверить `line-clamp`, `aspect-ratio`, skeleton-состояния на CLS
   (layout shift) при загрузке данных.
10. Обновить `docs/architecture/design-system.md`, если найдены
    расхождения с фактической реализацией (сама SKILL.md остаётся
    источником истины, документ только ссылается).

## Дизайн-заметки

Ничего нового не вводить — только приводить существующий UI в соответствие
с уже задокументированными правилами дизайн-системы.

**Важно — проверить перед стартом (добавлено 2026-08-05).** Администратор
отметил, что текущий UI недостаточно "строгий и красивый" (ориентир —
уровень полировки современных Next.js/Tailwind/shadcn LMS-платформ,
цветовая палитра Mentora при этом остаётся своей). Отдельная задача
strengthen-mentora-design-skill должна усилить
`.agents/skills/mentora-design/SKILL.md` (шкала теней/elevation, radius,
типографика, паттерны карточек/empty-state) ДО начала этой задачи. Если
на момент старта schedule/07 SKILL.md ещё не обновлён — начать с этого
обновления (или уточнить у администратора), а не проводить полировку по
старой, менее строгой версии документа.

## Критерии готовности

- Ни один список >10 элементов не анимирует width/height.
- `prefers-reduced-motion: reduce` заметно сокращает/отключает анимации.
- Полный keyboard-only проход по основным сценариям без потери фокуса.
- Lighthouse Accessibility ≥ 90 на ключевых страницах (предварительная
  проверка перед schedule/09).
- Скриншот-гейт: пройтись `review-animations`/`apple-design` skills по
  ключевым экранам (каталог, курс, редактор, плеер, checkout, дашборды),
  375/768/1280px, зафиксировать в
  `docs/qa-screenshots/schedule-07-polish-animations/`.

## Не входит

Тёмная тема (не входит в текущий дизайн-скоуп, если не оговорено отдельно).

## Коммит

`refactor: приведены анимации и доступность к стандарту дизайн-системы`
