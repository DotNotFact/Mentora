Создай страницу {PageName} на роуте {path}.

Страница тонкая: импортирует компоненты из features/, передаёт параметры
из роута, оборачивает в layout если нужно. Никакой бизнес-логики.

Роут: src/app/routes/{path}.tsx
