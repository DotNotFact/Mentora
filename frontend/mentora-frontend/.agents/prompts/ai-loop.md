Работай по AI Loop для задачи: {задача}.

Ценность: {value}. Критерии готовности: {acceptance criteria}. Границы:
{in scope}; не делаем: {out of scope}.

Фаза 1 — discovery: прочитай CLAUDE.md, AGENTS.md, docs/architecture/,
существующий код. Найди аналогичные компоненты/хуки/схемы.

Фаза 2 — plan: docs/plans/YYYY-MM-DD-{slug}.md. Для каждого шага: файлы,
критерий готовности, команда проверки. Код не меняй, жди approve.

Фаза 3 — execute после approve: шаг → проверка → результат → следующий шаг.

Фаза 4 — review: diff на нарушение правил CLAUDE.md, дубли,
несоответствия. Предложи commit-сообщение на русском, не коммить сам без
подтверждения.
